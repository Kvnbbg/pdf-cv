require "json"

Pod::Spec.new do |s|

  s.name         = "ReactNativeElementCore"

  s.version      = '1.0.0'

  s.summary      = "react native element, core mini api, core element"

  s.description  = <<-DESC
react native element, core mini api, core element, base
DESC

  s.homepage     = "https://onety.cn/sf/blog/blog/index.html"

  s.license      = "MIT"

  s.author       = { "wangtieshan" => "15003836653@163.com" }

  s.platform     = :ios, "8.0"

  #s.source       = { :git => "http://gitlab.shenmajr.com/tieshan.wang/RNCommon.git", :tag => s.version }

  # s.source       = { :path => "./ios", :tag => s.version }

  s.framework  = "UIKit", "Foundation"

  s.dependency 'React'

  s.dependency 'ZipArchive', '~> 1.4.0'

  s.source_files  = "api/*.{h,m,swift}", "event/*.{h,m,swift}", "mini/*.{h,m,swift}", "net/*.{h,m,swift}", "tool/*.{h,m,swift}", "ios/difflib/*.{c,h,m}"

  s.requires_arc = ["api/*.{h,m,swift}", "event/*.{h,m,swift}", "mini/*.{h,m,swift}", "net/*.{h,m,swift}", "tool/*.{h,m,swift}"]

end
