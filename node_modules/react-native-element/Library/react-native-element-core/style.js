/**
 * Created on 14:50 2019/03/06.
 * file name style
 * by wangtieshan
 */

/*
* style 为预定义的一些常用的CSS，减少一些重复的CSS代码，加快开发速度
* 为每一种CSS样式均提供了 extra 方法，可以将额外的 CSS 以参数传入，返回合并后的CSS
* 例：子视图绝对布局到父视图
* se.style.absFull
* se.style.absFull.extra({ backgroundColor: 'white' })
* */

export const EStyle = {
    /* 绝对布局，铺满整个父视图 */
    absFull: {
        position: 'absolute',
        left: 0,
        right: 0,
        bottom: 0,
        top: 0
    },
    /* 横向布局，且子视图均居中排列 */
    rowCenter: {
        flexDirection: 'row',
        alignItems: 'center'
    },
    /* 横向布局，且子视图最大排列 */
    rowStretch: {
        flexDirection: 'row',
        alignItems: 'stretch'
    },
    /* 居中 */
    center: {
        justifyContent: 'center',
        alignItems: 'center',
    }
};